{
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "name": "Medical",
  "type": "module",
  "scripts": {
    "prebuild": "cd src/Medical_frontend && npm install",
    "build": "cd src/Medical_frontend && npm run build",
    "start": "cd src/Medical_frontend && npm start",
    "test": "cd src/Medical_frontend && npm test"
  }
}
